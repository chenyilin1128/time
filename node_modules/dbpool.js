/**
 * 创建连接池，连接数据库.
 */

const mysql = require('mysql');

var pool = mysql.createPool({
    host:'127.0.0.1',
    user:'root',
    password:'',
    database:'mTime',
    connectionLimit:10
});

module.exports = pool;